{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "fastapi",
  "buildCommand": "cd frontend && npm install && npm run build && cd .. && rm -rf backend/frontend_dist && mkdir -p backend/frontend_dist && cp -R frontend/dist/. backend/frontend_dist/"
}
